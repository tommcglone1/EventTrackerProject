import { RookieCardPipe } from './rookie-card.pipe';

describe('RookieCardPipe', () => {
  it('create an instance', () => {
    const pipe = new RookieCardPipe();
    expect(pipe).toBeTruthy();
  });
});
